defmodule CercleApi.ImportContactFileTest do
  use CercleApi.ModelCase

  alias CercleApi.ImportContactFile

  # @valid_attrs %{content: "some content", content_type: "some content"}
  # @invalid_attrs %{}

  # test "changeset with valid attributes" do
  #   changeset = ImportContactFile.changeset(%ImportContactFile{}, @valid_attrs)
  #   assert changeset.valid?
  # end

  # test "changeset with invalid attributes" do
  #   changeset = ImportContactFile.changeset(%ImportContactFile{}, @invalid_attrs)
  #   refute changeset.valid?
  # end
end
